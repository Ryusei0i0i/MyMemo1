services:
  db:
    image: postgres:16
    container_name: mymemo1-db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: memo1_app
    ports:
      - "5432:5432"